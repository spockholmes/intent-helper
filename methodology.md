# 与AI达成共识：意图完整，显式化，冗余剪裁，呈现友好

## 引言

沟通的本质是信息传递，达成共识。模型能力越来越强，但信息传递方式仍是限制其有效性的因素。人与AI的沟通，目前还无法像人与人那样——实时观察、互动确认、随时澄清。AI存在上下文限制、幻觉等局限，信息传递中的偏差难以在过程中被自然消解。

本文提出达成共识的沟通四原则：意图完整，显式化，冗余剪裁，呈现友好。具体做法可能随模型演进部分过时，但把信息说清楚、说到位、说到点上，这个方向不会变。

四个要素各自解决一个问题：

要素 | 目的 | 对AI | 对人 
---|---|---|---
意图完整 | 信息不缺项 | 有足够信息执行任务 | 明确要做什么 
显式化 | 补全AI不知道的 | 减少猜测，降低随机输出 | 减少理解偏差 
冗余剪裁 | 核心不被淹没 | 注意力集中在关键约束上 | 文档简洁，快速抓重点 
呈现友好 | 文档与代码可读 | 结构清晰，容易解析 | 便于审核和阅读，代码可维护 

## 第一部分：现有信息传递方式的问题

与AI沟通，信息传递中常见三类问题。

### 问题一：逻辑散落，信息缺失

完整逻辑散落在不同文档里，信息本身也常有缺失——有些靠口头传递的内容，文档里根本没有。AI需要自己把散落的部分联系起来，缺失的部分只能靠猜测。

### 问题二：上下文里的噪声

上下文中塞了大量AI已知的内容（框架用法、基础语法），真正重要的约束反而被淹没。AI的注意力被分散，抓不住重点。

### 问题三：过时知识库与执行冲突

知识库可能滞后于实际做法，也可能包含AI已知的冗余内容。过时的内容与AI从训练数据中学到的做法冲突时，AI无法判断该遵循哪个。即便内容没问题，规范写长了，后面的部分也容易被忽略。

## 第二部分：意图完整

第一部分说了三个问题：逻辑散落、上下文有噪声、过时知识库与执行冲突。这些问题指向同一个根源——传递给AI的信息本身不够完整、清晰。

要解决这个问题，首先要明确意图是什么，以及如何把意图说完整。

### 2.1 意图的定义

意图，是想让AI做什么、为什么做、在什么条件下做。它回答三个问题：

做什么（What）：要产出什么结果

为什么做（Why）：解决什么问题

有什么约束（How）：必须遵守哪些限制

意图和指令不是一回事。指令是具体步骤——“先调A接口，再解析返回值”。意图是步骤背后的目的——“获取用户余额，看能不能参加活动”。意图清晰时，AI可以自己选择怎么实现；只有指令时，AI只能照着做，遇到没说到的情况就不知道怎么处理了。

### 2.2 意图的作用机制

意图的清晰度，直接影响AI对任务的理解和执行质量。这个影响通过三个机制发挥作用：

第一，意图缩小AI的搜索范围。 AI执行任务，是在它学过的知识里找匹配的方案和路径。意图越明确，找到正确方案的概率越高。意图模糊，AI就只能在大范围内猜测。

第二，意图决定AI推断的方向。 信息不完整时，AI会用常见模式来补。比如你说“统计销售额”，没说是“实付”还是“应付”，AI就会自己选一个，不一定是你想要的。

第三，意图是判断和调整的参照。 任务执行出现偏差时，AI需要知道“目标是什么”才能修正方向。没有这个参照，AI无法判断对错，只能根据模糊的反馈乱试。

所以，把意图说清楚，是决定AI协作成效的基础。

### 2.3 用5W1H把意图说完整

意图说完整需要一个结构化的工具。5W1H是六个维度，按需填充：

维度 | 含义 | 类型 | 说明 
---|---|---|---
Why | 为什么做 | 按需补充 | 帮助AI理解背景和判断依据，必要时说明 
What | 做什么 | 意图核心 | 每次都要说清楚，是沟通的起点 
How | 特殊约束或约定 | 按需补充 | 只有当存在特殊约定时才说明，如“响应时间不超过200ms”“用groovy实现” 
Who | 谁用 | 按需补充 | 涉及权限和角色时 
Where | 在哪个场景 | 按需补充 | 涉及具体位置或系统时 
When | 什么时候触发 | 按需补充 | 涉及时间或频率时 

简单的任务，把What说清楚就够了。复杂的任务再补上其他维度。

### 2.4 每个维度怎么填

Why：需要时说明问题背景和解决目的，帮助AI在遇到模糊点时做出正确判断。如果任务本身足够清晰，可以省略。

What：说明要产出什么。这是意图的主体，每次都要说清楚。如果任务范围容易模糊，可以补充说明“不包括什么”来限定边界。

How：补充不影响意图主体的特殊要求，比如“响应时间不超过200ms”“用groovy实现”。这类要求不会改变“做什么”本身，只影响实现的约束或方式。如果没有特殊要求，可以不写。

Who/Where/When：只有任务确实依赖这些信息时才填，否则不写。

## 第三部分：显式化

意图说完整之后，还需要把AI原本不知道的信息明确告诉它。这就是显式化。

### 3.1 什么是显式化

人与人沟通时，可以通过手势、演示、现场上下文等方式达成理解。但AI不具备这些条件——它只能依赖你明确告诉它的内容。

显式化，就是把模糊的说清楚，把隐含的写出来，让AI拿到它需要的信息。比如“统计销售额”，需要说明白是“统计orders表中status='paid'的实付金额”；“获取用户信息”，需要说明白要“用户的手机号和邮箱字段”。

### 3.2 显式化是意图完整性的关键

告诉AI“做什么”之外，还要告诉它“用什么”和“怎么取”。如果这些信息没说清，AI就只能猜测。

显式化的内容，包括完整的逻辑、未明说的前提、任务或场景特有的约定等——这些是AI自己没法知道的东西。

不需要写的是AI已经知道的东西。比如编程语言的基础语法、主流框架的常规用法，以及AI能从代码或配置里自己读出来的东西。

## 第四部分：冗余剪裁

意图说完整了，也显式化了，接下来的问题是：所有内容都需要写出来吗？

### 4.1 什么是冗余剪裁

剪裁，指去掉AI已经知道或能自己查到的内容，只保留它不知道的。剪裁的目标是让核心意图不被多余的内容淹没，对人和AI都友好。

剪裁遵循的原则是：如无必要，勿增实体——不必要的内容不加，必要的内容尽量精简。

比如意图文档中不应包含具体实现代码——那是实现阶段的事情。文档只需保留必要的信息：逻辑、数据口径、特殊架构要求等。信息过多会造成信息爆炸，增加人工审核成本和达成共识的成本。

### 4.2 为什么要剪裁

对于AI和人，注意力都是有限的。内容越多，核心信息越容易被淹没。

AI的上下文窗口有限，内容越长，后半部分的权重越低。把已知内容塞进去，真正重要的意图就得不到足够的关注。

冗余信息等于噪声。常识性的内容占了大篇幅，模型可能误以为那是重点，反而忽略了真正关键的信号。

过时的外部信息引入冲突。过时的外部信息与AI从训练数据中学到的做法不一致时，模型无法判断该遵循哪个——它不知道这是用户的特殊约定，还是用户沿用了过时的做法。

对人而言，信息过多也会增加阅读和审核的负担，降低沟通效率。

### 4.3 剪裁的适用范围

剪裁的视角应站在AI这一侧——AI是最终的执行者，文档提供的信息应当以“AI执行任务所必需”为判断标准。

剪裁的前提是AI已经具备必要的上下文。如果AI对当前任务的背景、技术手段、已有实现等还缺乏了解，就需要在意图中提供足够的“脚手架”信息，比如使用什么技术手段、依赖哪些工具或库。

一旦AI通过读取代码、配置或目录结构掌握了这些上下文，后续沟通中就不再需要重复。

剪裁原则的适用范围是：在AI已具备必要上下文的前提下，去掉AI已知的冗余信息。

## 第五部分：呈现友好

剪裁之后，保留下来的内容需要用合适的方式呈现，让核心信息处在AI更容易关注到的位置，同时对人也可读。

### 5.1 呈现原则

呈现遵循两条原则：

核心前置，分层组织。 把最重要的放在最前面；复杂内容按意图分层，主文档放总体，子文档放细节，每层只关注自己的职责。

语言清晰。 文档语言应自然、逻辑清晰，关键概念要显式化、无歧义，引用外部内容时给出明确路径。

### 5.2 总分结构

按意图划分层次，合理组织目录结构：

简单任务：一份文档写清楚，用合理的章节组织，每章标题概括内容。

复杂任务：主文档放整体意图（全局目标和范围），子文档放具体逻辑。子项复杂时继续拆分，形成多层次结构。主文档只留引用和简要说明。

**结构随意图梳理逐步调整**：不强求一开始就定好结构，也不强求拆到几层。开始时可能一个文档就够，随着内容增多逐步拆分。

每一层只关注自己的职责，主文档不重复子文档内容，只提供索引和引用。

这样做的好处是：阅读者（包括AI）先读主文档就能抓住主线，需要细节再往下翻。改某个子项时也不影响整体。

### 5.3 代码组织

大多数情况下，我们对代码的可读性也有要求，要求代码对人友好，便于理解、审核和维护。产出代码时也可借鉴总分结构及其拆分方式——类/方法按意图命名，复杂逻辑按意图拆分为子类/方法，主类/方法负责总体协调，便于人审核和AI理解。

方法名直接表达意图，判断条件显式化，不依赖注释理解代码。

## 第六部分：用Skill矩阵承载方法论

前述四个原则是方法论的核心，在具体实践中，可以把这套方法论作为引擎，搭建Skill矩阵，覆盖意图从构建、执行到沉淀的完整流程。

### 6.1 意图识别与澄清

从草稿出发，识别意图的复杂度：

轻量模式：澄清后直接执行

复杂意图：沉淀为文档，完善后再实施

Skill 入口先判断走哪个模式，进入任一模式时主动向用户说明澄清边界。

### 6.2 意图执行

依据意图生成代码。代码遵循总分层次——类/方法按意图命名，复杂逻辑按意图拆分，主类/方法负责总体协调。

### 6.3 测试验证

对执行结果进行验证，确认意图是否被正确理解、代码是否符合预期。

### 6.4 公共内容沉淀

在具体实践中，可以把公共的内容沉淀下来，沉淀下来的文档，其组织形式遵循前述四个原则。这部分由AI协助，依据方法论一定程度自动维护。

#### 6.4.1 知识库分层

知识库分两层：

- **项目层**：存放项目公共约定、表结构等项目相关内容
- **用户层**：存放环境信息、个人知识库，以及不适合放项目层的内容

各层之上维护一个统一入口，索引所有子 wiki 及其定位、何时使用、如何查阅、引用、维护。wiki 类别可根据实际情况扩展。

#### 6.4.2 公共约定

用户主动提出，或AI在沟通中识别出需要与AI约定的内容，提醒用户是否纳入公共约定。用户确认后，按总分层次维护——总览负责索引，子文档记录具体约定。只记录必要的，不堆积冗余。

#### 6.4.3 数据表知识库

当前意图涉及表结构时，已有表直接引用；新增表则设计好后自动纳入知识库；需要修改时同步更新。采用总分结构——总览负责索引，表文档记录表名、表说明、表结构。作为建表和团队沟通的依据，意图文档直接引用，不必重复说明。

#### 6.4.4 用户层知识库

环境信息、个人知识库、不适合放项目层的内容，存放到用户层知识库。位于用户主目录，不在项目仓库内，不会被 Git 跟踪。

#### 6.4.5 动态维护

AI 在沟通中主动维护 wiki：
- 发现内容过时 → 提醒调整
- 发现内容多余 → 提醒清理
- 发现不符合四原则 → 提醒调整
- 发现需要纳入 → 提醒添加
- 发现内容不适合放项目层（含敏感信息）→ 提醒改放用户层

## 第七部分：意图文档的价值不止于写代码

一份写得清楚的意图文档，在软件开发的多个阶段都能发挥作用。

设计时：在AI辅助下，从需求草稿出发，与AI一起理清需求，逐步形成完整的意图文档。

评审时：意图文档本身就是评审材料。逻辑完不完整，在文档层面就能检查，不用等代码写完。一致的术语、明确的层级，也让团队评审更高效。

开发时：依据意图文档生成代码，代码可读性高，便于人审核。也可以先让AI生成测试代码，跑通后再迭代修复问题，形成文档驱动的开发闭环。

意图变化时：先更新意图文档中的相关逻辑描述，然后通过文档说明改动内容。AI基于更新后的文档定位代码并调整。文档成为变化的记录和意图来源。

测试时：逻辑和数据口径都有明确定义，测试人员可以对照文档检查数据，也可以让AI根据文档自动生成测试数据并验证结果。

Wiki 维护时：意图中涉及的公共内容（约定、表结构、环境信息）按需沉淀到对应层；敏感信息和个人知识库沉淀到用户层。由 AI 协助识别、提醒、维护。

Skill开发：Skill也是一种意图文档，描述可复用任务的意图，按方法论的形式组织。
